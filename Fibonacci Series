#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
           int n,i,r1=0,r2=1,r3;
    scanf("%d",&n);
    for(i=2;i<=n;i++)
    {
        r3=r1+r2;
    r1=r2;
        r2=r3;
    }
    printf("%d",r2);
    return 0;
}
