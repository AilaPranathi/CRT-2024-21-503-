#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i=0,sum=0;
    while(i>-1)
    {
        scanf("%d\n",&i);
        sum=sum+i;
    }
    printf("%d",sum+1);
    return 0;
}
