#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int n,i,d=9;
    scanf("%d",&n);
    while(n>0)
    {
        i=n%10;
        if(i<d){
            d=i;
        }
        n=n/10;
    }
    
    printf("%d",d);
    return 0;
}
